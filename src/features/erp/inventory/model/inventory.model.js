/**
 * Inventory Database Model (Prisma)
 * Maps to the "inventoryTransaction" model in prisma schema.
 */
export const InventoryModelDef = {
  modelName: 'inventoryTransaction',
  tableName: 'inventory'
};
